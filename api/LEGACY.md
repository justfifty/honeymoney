# ⚠️ Legacy prototype — not part of the competition MVP

This `api/` folder holds an **earlier FastAPI + SQLite** prototype from before the
architecture was finalized.

The competition MVP uses a **zero permanent backend** design: Next.js 16 serverless
routes ([`web/`](../web)) + Supabase Postgres knowledge graph + Gemini (REST) +
Telegram. See [`PLAN.md`](../PLAN.md) for the current architecture.

**This code is retained for reference only.** It is not deployed, not wired into the
app, and not part of the pitch. It can be deleted once the team is comfortable.
